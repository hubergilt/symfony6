{% extends 'base.html.twig' %}

{% block title %}Arrendatario{% endblock %}

{% block body %}
    <h1>Arrendatario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arrendatario.id }}</td>
            </tr>
            <tr>
                <th>Nombres</th>
                <td>{{ arrendatario.nombres }}</td>
            </tr>
            <tr>
                <th>Paterno</th>
                <td>{{ arrendatario.paterno }}</td>
            </tr>
            <tr>
                <th>Materno</th>
                <td>{{ arrendatario.materno }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ arrendatario.dni }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arrendatario_index') }}">back to list</a>

    <a href="{{ path('app_arrendatario_edit', {'id': arrendatario.id}) }}">edit</a>

    {{ include('arrendatario/_delete_form.html.twig') }}
{% endblock %}
