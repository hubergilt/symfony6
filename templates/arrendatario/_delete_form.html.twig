<form method="post" action="{{ path('app_arrendatario_delete', {'id': arrendatario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arrendatario.id) }}">
    <button class="btn">Delete</button>
</form>
