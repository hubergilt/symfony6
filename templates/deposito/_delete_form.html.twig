<form method="post" action="{{ path('app_deposito_delete', {'id': deposito.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deposito.id) }}">
    <button class="btn">Delete</button>
</form>
