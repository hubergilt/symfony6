{% extends 'base.html.twig' %}

{% block title %}Ambiente{% endblock %}

{% block body %}
    <h1>Ambiente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ambiente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ambiente.nombre }}</td>
            </tr>
            <tr>
                <th>Piso</th>
                <td>{{ ambiente.piso }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ ambiente.precio }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ ambiente.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ambiente_index') }}">back to list</a>

    <a href="{{ path('app_ambiente_edit', {'id': ambiente.id}) }}">edit</a>

    {{ include('ambiente/_delete_form.html.twig') }}
{% endblock %}
